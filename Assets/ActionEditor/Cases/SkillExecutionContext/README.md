# 技能执行上下文验证案例

该目录验证施法者、目标、世界坐标、方向、共享战斗服务以及 `SkillPlayer` 上下文绑定。

1. 在任意 GameObject 上添加 `SkillExecutionContextCase`。
2. 右键组件标题并执行“验证技能执行上下文”。
3. 确认 Console 输出“技能执行上下文验证通过。”

删除该案例目录不会影响 ActionEditor Runtime 或 Editor 程序集。
