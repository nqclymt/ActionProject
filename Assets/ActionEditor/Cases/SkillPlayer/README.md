# SkillPlayer 验证案例

该目录用于验证运行时 `SkillPlayer` 的播放状态机，删除后不会影响 ActionEditor。

1. 在任意 GameObject 上添加 `SkillPlayerCase`。
2. 右键组件标题。
3. 执行“验证 SkillPlayer 播放控制”。
4. 确认 Console 输出“SkillPlayer 运行时播放控制验证通过。”

案例覆盖播放、暂停、停止、打断、跳转、多轮循环、完成事件和连续时间段求值。
